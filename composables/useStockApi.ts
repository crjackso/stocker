// import IStockApi from '~/lib/IStockApi'

// export const useStockApi = () => {
//   const portfolioDividends = () => {
//     const { data } = useFetch('/api/stocks', {
//       // onResponse({ response }) {
//       //   const { _data } = response
//       //   const logs = _data.stockDividendLogs.map(
//       //     (log: any) => new StockDividendLog(log)
//       //   )
//       //   // return new StockDividendLogs(logs)
//       // }
//     })

//     const { stockDividendLogs } = data

//     const logs = stockDividendLogs.map((log: any) => new StockDividendLog(log))
//     return new StockDividendLogs(logs)
//   }

//   return { portfolioDividends }
// }
